sudo: true
language: java
notifications:
  email: false
jdk:
  - oraclejdk8

services:
  - mongodb
before_install:
  - chmod +x gradlew
install:
  - ./gradlew clean compileJava
script:
  - ./gradlew clean test jacocoTestReport coveralls -Dspring.profiles.active=ci
after_success:
  - ./gradlew clean coveralls
branches:
  only:
    - master
